/**
 * Configuração centralizada do TanStack Router
 */
export const routerConfig = {
  defaultPreload: "intent" as const,
  defaultPreloadDelay: 100,
  defaultPendingMs: 1000,
  defaultPendingMinMs: 500,
};
